function convertCase(changeCase){
    if(changeCase=="upper"){
        document.formm.enter.value = document.formm.enter.value.toUpperCase()
    }
    else{
        document.formm.enter.value = document.formm.enter.value.toLowerCase()
    }
}

